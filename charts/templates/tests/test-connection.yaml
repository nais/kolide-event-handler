apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kolide-event-handler.fullname" . }}-test-connection"
  labels:
    {{- include "kolide-event-handler.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kolide-event-handler.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
